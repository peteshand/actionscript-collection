package net.peteshand{	import flash.display.MovieClip;	import flash.events.Event;	import flash.display.LoaderInfo;	import flash.display.StageAlign;	import net.peteshand.utils.StageAlignment;		public class BaseLayout extends MovieClip	{		public var topleft:MovieClip;		public var topmiddle:MovieClip;		public var topright:MovieClip;		public var bottomleft:MovieClip;		public var bottommiddle:MovieClip;		public var bottomright:MovieClip;		public var middle:MovieClip;		public var right:MovieClip;		public var left:MovieClip;				private var stageAlignment:StageAlignment;		private var minX:int;		private var maxX:int;		private var midX:int;		private var minY:int;		private var maxY:int;		private var midY:int;				public function BaseLayout():void		{			topleft = new MovieClip();			topmiddle = new MovieClip();			topright = new MovieClip();			bottomleft = new MovieClip();			bottommiddle = new MovieClip();			bottomright = new MovieClip();			middle = new MovieClip();			right = new MovieClip();			left = new MovieClip();						addEventListener(Event.ADDED_TO_STAGE, OnAddToStage);		}				public function OnAddToStage(event:Event):void		{			removeEventListener(Event.ADDED_TO_STAGE, OnAddToStage);												/*addGraphic(topleft);			addGraphic(topmiddle);			addGraphic(topright);			addGraphic(bottomleft);			addGraphic(bottommiddle);			addGraphic(bottomright);			addGraphic(middle);			addGraphic(right);			addGraphic(left);*/						addChild(middle);			addChild(topmiddle);			addChild(topright);			addChild(bottomright);			addChild(bottomleft);			addChild(bottommiddle);			addChild(right);			addChild(left);			addChild(topleft);						stageAlignment = new StageAlignment(this);						addEventListener(Event.ENTER_FRAME, a);			function a(event:Event):void			{				removeEventListener(Event.ENTER_FRAME, a);				//stageAlignment.calculate();				setPlacement();			}			//setPlacement();						stage.addEventListener(Event.RESIZE, OnResize);					}				private function addGraphic(mc:MovieClip):void		{			mc.graphics.beginFill(0xFFFFFF*Math.random());			mc.graphics.drawRect(-10,-10,20,20);			mc.graphics.endFill();		}		private function OnResize(event:Event):void		{						setPlacement();		}		private function setPlacement():void		{			minX = stageAlignment.minX;			maxX = stageAlignment.maxX;			midX = stageAlignment.midX;			minY = stageAlignment.minY;			maxY = stageAlignment.maxY;			midY = stageAlignment.midY;						topleft.x = minX;			topleft.y = minY;						topright.x = maxX;			topright.y = minY;						bottomleft.x = minX;			bottomleft.y = maxY;						bottomright.x = maxX;			bottomright.y = maxY;						topmiddle.x = midX;			topmiddle.y = minY;						bottommiddle.x = midX;			bottommiddle.y = maxY;						middle.x = midX;			middle.y = midY;						right.x = maxX;			right.y = midY;						left.x = minX;			left.y = midY;		}	}}